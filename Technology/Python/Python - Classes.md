It's a `blueprint`

```python
class Car
	def __init__(self, color: str, horsepower: int) -> None:
		self.color = color
		self.horsepower = horsepower
		
volvo: Car = Car('Rec', 200)
```

## Methods

I `Method` it's just a function declared inside a `class`.

```python
...
def drive(self) -> None:
	print(f'{self.brand} is driving!')
```

#programming #python 